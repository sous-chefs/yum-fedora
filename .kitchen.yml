driver:
  name: dokken
  chef_version: 12.5.1

transport:
  name: dokken

provisioner:
  name: dokken

verifier:
  root_path: '/opt/verifier'
  sudo: false

platforms:
- name: fedora-22
  driver:
    image: fedora:22

suites:
  - name: default
    run_list:
      - recipe[yum-fedora::default]
